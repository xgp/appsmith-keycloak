# appsmith+keycloak docker 

docker build -t appsmith/appsmith-keycloak .
docker-compose -f docker-compose.yml up

## Notes
- not possible to get keycloak logs into the "stacks" volume, as that dir gets setup by the appsmith entrypoint
